===
timer: anonymous
===

~{25%minutes}

---

(recipe
  (step
    (timer
      (quantity
        (quantity_value)
        (percent_separator)
        (quantity_unit)))))

====================================
timer: a timer must have a quantity!
====================================

~timer!

---

(recipe
  (step
    (text
      (text_content))))

=========================================================
timer: a timer must have a quantity! a note is not enough
=========================================================

~timer!(not!)

---

(recipe
  (step
    (text
      (text_content))))

===
timer: multiple
===

Cook for ~{5%minutes}, then simmer ~{20%minutes}

---

(recipe
  (step
    (text
      (text_content))
    (timer
      (quantity
        (quantity_value)
        (percent_separator)
        (quantity_unit)))
    (text
      (text_content))
    (timer
      (quantity
        (quantity_value)
        (percent_separator)
        (quantity_unit)))))

===
timer: named
===

~eggs{3%minutes}

---

(recipe
  (step
    (timer
      (timer_name)
      (quantity
        (quantity_value)
        (percent_separator)
        (quantity_unit)))))

===
timer: anonymous with note
===

~{10%minutes}(or until golden brown)

---

(recipe
  (step
    (timer
      (quantity
        (quantity_value)
        (percent_separator)
        (quantity_unit))
      (preparation
        (preparation_content)))))

===
timer: named with note
===

~rest{30%minutes}(covered)

---

(recipe
  (step
    (timer
      (timer_name)
      (quantity
        (quantity_value)
        (percent_separator)
        (quantity_unit))
      (preparation
        (preparation_content)))))

===
timer: with hours unit
===

~marinate{2%hours}

---

(recipe
  (step
    (timer
      (timer_name)
      (quantity
        (quantity_value)
        (percent_separator)
        (quantity_unit)))))

===
timer: with decimal hours unit
===

~{1.5%hours}

---

(recipe
  (step
    (timer
      (quantity
        (quantity_value)
        (percent_separator)
        (quantity_unit)))))

===
timer: value with no unit
===

~{5}

---

(recipe
  (step
    (timer
      (quantity
        (quantity_value)))))
